Load `modes/audit.md` and follow its instructions to audit the full extraction pipeline for integrity.

Check tracker consistency, verify all expected files exist, flag any blockers, and write the audit report to `reports/audit-report.md`. This must pass cleanly before running /review-ops synthesis.

$ARGUMENTS
